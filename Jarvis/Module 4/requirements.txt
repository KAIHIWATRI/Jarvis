# JARVIS TTS Module — Requirements & Setup

## Python dependencies
edge-tts>=6.1.9
pygame>=2.5.2
faster-whisper>=1.0.0
httpx>=0.27.0
customtkinter>=5.2.0   # for UI bridge

## Optional / already in stdlib
# asyncio, threading, heapq, logging, tempfile, dataclasses, enum, pathlib

## Install
pip install edge-tts pygame faster-whisper httpx customtkinter

## Ollama
# Install from https://ollama.com and pull a model:
# ollama pull mistral
